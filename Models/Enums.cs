namespace TestCase.Models
{
    public enum CouponType
    {
        Fixed,
        Percentage
    }

    public enum CouponStatus
    {
        Active,
        Expired,
        Cancelled,
        Used
    }
}
